# Environment variables for local development
# For Railway deployment, set the DATABASE_URL in your Railway dashboard

# Example format for local PostgreSQL setup (optional):
# DATABASE_URL=postgresql://username:password@localhost:5432/torba_text_storage

# For Railway deployment, this will be automatically set
# DATABASE_URL=your-railway-database-url
